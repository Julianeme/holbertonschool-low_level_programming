#include "holberton.h"

/**
 * print_alphabet - function that prints the alphabet in lower case.
 *
 */

void int _isupper(int c)
{
	if (c >= 65 && c <= 9)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
