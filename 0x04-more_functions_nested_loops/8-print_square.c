#include "holberton.h"

/**
 * print_square - prints a square
 *
 *@n: int, the size of the square
 */

void print_square(int size)
{
	int x;

	if (n > 0)
	{
		for (x = n; x < 0; x++)
		{
			for (y = 0; y < n; y++)
			{
				_putchar(35);
			}
			_putchar(10);
		}
	}
	else
	{
		_putchar(10);
	}
}
